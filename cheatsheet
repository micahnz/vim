ysiw" //create surround inside word, wrap word with "
cs"' //change surround " with ', replace " with '

:e filename //open file
:tabe/tabedit filename //open file in new tab

:w filename //save file
:q //quit
:wq //write and quite
:x //same as above

e //end of word
E //end of word with whitespace
b //beginning of word
B //beginning of word with whitespace

H //to first line of screen
M //to middle of screen
L //to last line of screen

:{n} //jump to line

i //insert before cursor
I //insert at start of line

a //insert after cursor
A //insert at end of line

o //new line below + insert
O //new line above + insert

C //change the rest of the line

r //replace one character
R //replace mode

x //delete under cursor
X //delete before cursor

v //highlight characters
V //highlight lines

~ //change case

> //indent
< //undent
c //chance
y //copy (aka yank)
d //cut 
yy or :y //copy line
dd or :d //cut line
p //paste
P //paste before

u //undo
U //undo all changes on current line

R //redo

/pattern //search
n //next
N //prev

//REPLACE
:{range}s/food/bar/{args}

//range
nothing = current line only
number = for line number
% = whole file

//args
g = replace all
i = ignore case
I = dont ignore case
c = confirm each

,w (:w!) //fast save

,bc (:Bclose) //close current buffer
,ba (:1,1000 bc! //close all buffers

,tf (:tabfirst) //go to first tab
,tl (:tablast) //go to last tab
,tn (:tabnew) //new tab
,to (:tabonly) //close other tabs
,tc (:tabclose) //close tab
,tm (:tabmove/:tabm) //move tab 0 = first null = last
,tj (:tabnext) //jump to tab
,te (:tabedit) //open tab in same dir, tab edit

gt //go to next tab
gT //go to prev tab
{i}gt //go to tab at i


,ss (:setlocal spell!) //toggle spell check
,sn (]s) //spell next
,sp ([s) //spell prev
,sa (zg) //add word
,s? (z=) //show options

,nn (NERDTreeToggle) //toggle nerd tree

,f (MRU) //most recently used files
ctrl + f/b //cycle modes
ctrl + j/k //navigate
ctrl + t //open in new tab
ctrl + v/x //open in split screen (tall/wide)
ctrl + o //open selected with promp
ctrl + z //mark/unmark multiple files
